# Getting Started with Create React App

this App has FE and BE right after cloning please hit npm install and follow the following steps

## Available Scripts

In the project directory, you can run:

### `npm start`

Runs the app in the development mode.\
Open [http://localhost:3000](http://localhost:3000) to view it in your browser.

The page will reload when you make changes.\
You may also see any lint errors in the console.

## for Backend

- just run npm run dev
  open [http://localhost:5000] on postman so you can check the API

## for Database ( MongoDB has been used )

- exported the database so it can be imported in mongoDB compass locally and get exactly the same setup i have to complete the test
